version: 1
applications:
  - appRoot: packages/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm i
        build:
          commands: []
      artifacts:
        files:
          - '*/**'
      cache:
        paths:
          - node_modules/**/*
