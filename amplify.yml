version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ./packages/web
        - npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: pacakges/web/dist
    files:
      - '*/**'
  cache:
    paths:
      - node_modules/**/*
