version: 1
applications:
  - appRoot: packages/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm i
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        discard-paths: yes
        files:
          - '*/**'
      cache:
        paths:
          - node_modules/**/*
