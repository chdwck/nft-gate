version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ./packages/web
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
  cache:
    - node_modules
