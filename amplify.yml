version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ./packages/web
    build:
      commands:
        - npm run build
  artifacts:
    files:
      - **/*
    baseDirectory: dist
  cache:
    - node_modules
