version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ./packages/web
        - npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: ./dist
    files:
      - '*/**'
  cache:
    paths:
      - node_modules/**/*
